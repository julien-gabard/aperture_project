const initialState = {
  /** Contains item list */
  articles: [],
};

const user = (state = initialState, action = {}) => {
  switch (action.type) {
    default: return state;
  };
};

export default user;